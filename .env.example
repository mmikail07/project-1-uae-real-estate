# Copy to .env and fill in. Never commit .env.

# geopy / Nominatim requires a unique user-agent string per usage policy
NOMINATIM_USER_AGENT=uae-real-estate-portfolio-mohammad

# Optional: FRED API key for macro indicators (free at https://fred.stlouisfed.org)
FRED_API_KEY=

# Path overrides (rarely needed; src/config.py resolves defaults)
DLD_DB_PATH=
